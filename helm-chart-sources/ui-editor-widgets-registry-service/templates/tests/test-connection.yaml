apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ui-editor-widgets-registry-service.fullname" . }}-test-connection"
  labels:
    {{- include "ui-editor-widgets-registry-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ui-editor-widgets-registry-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
