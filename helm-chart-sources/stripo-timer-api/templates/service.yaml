apiVersion: v1
kind: Service
metadata:
  name: {{ include "stripo-timer-api.fullname" . }}
  labels:
    {{- include "stripo-timer-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: 8081
      targetPort: actuator
      protocol: TCP
      name: actuator
  selector:
    {{- include "stripo-timer-api.selectorLabels" . | nindent 4 }}
