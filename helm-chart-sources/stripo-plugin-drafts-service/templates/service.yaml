apiVersion: v1
kind: Service
metadata:
  name: {{ include "stripo-plugin-drafts-service.fullname" . }}
  labels:
    {{- include "stripo-plugin-drafts-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stripo-plugin-drafts-service.selectorLabels" . | nindent 4 }}
