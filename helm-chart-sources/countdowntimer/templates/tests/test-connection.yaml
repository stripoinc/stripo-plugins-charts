apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "countdowntimer.fullname" . }}-test-connection"
  labels:
    {{- include "countdowntimer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "countdowntimer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
