apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stripo-url-validator-service.fullname" . }}-test-connection"
  labels:
    {{- include "stripo-url-validator-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stripo-url-validator-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
