apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "convo-core-chat-server.fullname" . }}-test-connection"
  labels:
    {{- include "convo-core-chat-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "convo-core-chat-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
