apiVersion: v1
kind: Service
metadata:
  name: {{ include "stripo-security-service.fullname" . }}
  labels:
    {{- include "stripo-security-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stripo-security-service.selectorLabels" . | nindent 4 }}
